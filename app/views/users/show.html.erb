<div class="row p-3 p-lg-5 mt-4 mx-auto card text-center profile-card">
        <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-chat/ava3.webp" alt="avatar"
          class="rounded-circle img-fluid mx-auto" style="width: 150px;">
        <h1 class="my-3"><%= current_user.full_name.capitalize %></h1>
        <p class="text-muted mb-1 font"><%= current_user.role.capitalize %></p>
        <p class="text-muted mb-1 font"><%= current_user.email %></p>
        <p class="text-muted mb-1 font"><%= current_user.institution.capitalize %></p>
        <div class="d-flex justify-content-center mb-2">
          <button type="button" class="btn btn-primary m-2">My Exams</button>
          <button type="button" class="btn btn-outline-primary ms-1 m-2">Create an Exam</button>
        </div>
</div>
